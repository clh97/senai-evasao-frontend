export default class Class {
  constructor(id, schoolId, className, period, status) {
    this.id = id;
    this.schoolId = schoolId;
    this.className = className;
    this.period = period;
    this.status = status;
  }
}